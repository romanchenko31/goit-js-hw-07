/*const list = document.querySelector('#categories');

const firstItem = list.firstElementChild;
const title = firstItem.querySelector('h2');
console.log(`Категория: ${title.textContent}`);
const lastItem = firstItem.lastElementChild;
const qount = lastItem.querySelectorAll('li');
console.log(`Количество: ${qount.length}`);

const secondEl = firstItem.nextElementSibling;
const secondTitile = secondEl.querySelector('h2');
console.log(`Категория: ${secondTitile.textContent}`);
const secondQount = secondEl.querySelectorAll('li').length;
console.log(`Количество: ${secondQount}`);

const lastChild = list.lastElementChild;
const lastTitle = lastChild.querySelector('h2');
console.log(`Категория: ${lastTitle.textContent}`);
const lastEl = lastChild.querySelectorAll('li').length;
console.log(`Количество: ${lastEl}`);
*/
const list = document.querySelector('input');
console.log(list.type);
